<div class="">
  <%= render 'form' %>
  <!-- <%= form_with(model: @picture, local: true) do |f| %>
  <%= f.text_field :content, placeholder:"その気持ちをシェアしよう"%>
  <%= f.file_field :image %>
  <%= f.submit %>
  <% end %> -->
</div>
